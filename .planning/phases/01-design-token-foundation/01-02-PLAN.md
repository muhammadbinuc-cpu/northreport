---
phase: 01-design-token-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified: []
autonomous: false
requirements:
  - DTKN-01
  - DTKN-02
  - DTKN-03
  - DTKN-04
  - DTKN-05
  - DTKN-06
  - TYPO-01
  - TYPO-02
  - TYPO-03
  - TYPO-04
  - A11Y-01
  - A11Y-02

must_haves:
  truths:
    - "Opening any page shows Warm Cream backgrounds with Dark Charcoal text and Deep Maroon accents"
    - "Cards are visually distinct from the cream background (white surface + shadow)"
    - "No dark/glassmorphism remnants visible on any page"
    - "Typography has clear size hierarchy across headings and body text"
    - "Status/severity chips are readable on both cream and white backgrounds"
  artifacts: []
  key_links: []
---

<objective>
Run automated CSS audits and then visually verify that the token system migration produces the correct visual output across all pages.

Purpose: The token system changes propagate to 45+ components automatically. This plan confirms that the propagation works correctly and no visual regressions exist before proceeding to Phase 2.

Output: Verified token system with audit results documented.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-token-foundation/01-CONTEXT.md
@.planning/phases/01-design-token-foundation/01-RESEARCH.md
@.planning/phases/01-design-token-foundation/01-01-SUMMARY.md

@app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run automated CSS token audits</name>
  <files></files>
  <action>
Run the following audit commands and report results:

1. **Build check:** `npm run build` -- must succeed clean
2. **Lint check:** `npm run lint` -- must pass
3. **Token completeness:** Verify all 8 brand palette primitives exist in :root: `grep -c 'palette-maroon\|palette-maroon-deep\|palette-charcoal\|palette-cream\|palette-ivory\|palette-white\|palette-rose\|palette-slate' app/globals.css`
4. **@theme inline completeness:** Verify @theme inline contains color-surface-*, color-text-*, color-brand-*, color-status-*: `grep -c 'color-surface-\|color-text-\|color-brand-\|color-status-' app/globals.css`
5. **Legacy alias preservation:** Verify --paper-base, --paper-elevated, --paper-warm, --ink-primary, --ink-secondary, --ink-muted all exist: `grep -c 'paper-base\|paper-elevated\|paper-warm\|ink-primary\|ink-secondary\|ink-muted' app/globals.css`
6. **Glass-card migration:** Verify .glass-card NOT in animations.css and IS in globals.css: `grep -l 'glass-card' app/globals.css components/landing/animations.css`
7. **No backdrop-blur in globals.css card classes:** `grep 'backdrop-filter' app/globals.css` should return nothing
8. **Font import removed:** `grep '@import.*googleapis' app/globals.css` should return nothing
9. **Shadow-pressed defined:** `grep -- '--shadow-pressed:' app/globals.css` should return the definition
10. **Status colors updated:** Verify #B91C1C appears for critical (not #EF4444): `grep 'status-critical' app/globals.css`
11. **Muted Rose annotation:** `grep -A1 'palette-rose' app/globals.css` should show DECORATIVE ONLY comment
12. **Dark-surface class exists:** `grep '.dark-surface' app/globals.css`
13. **Prose-width utility exists:** `grep 'prose-width' app/globals.css`

Report all results. If any audit fails, fix the issue in globals.css before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>cd /Users/muaaz/northreport && npm run build 2>&1 | tail -3 && npm run lint 2>&1 | tail -3</automated>
  </verify>
  <done>
    - All 13 audit checks pass
    - Build succeeds
    - Lint passes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of brand palette across all pages</name>
  <action>
Present the user with verification instructions for the complete CSS token system migration.

What was built: Complete CSS token system migration from dark glassmorphism to NorthReport brand palette (maroon/cream/charcoal). All :root variables, @theme inline, typography classes, component CSS classes, shadow system, and status colors have been updated. The .glass-card class has been migrated from animations.css to globals.css as a solid white surface.

How to verify:
1. Run `npm run dev` and open http://localhost:3000
2. **Landing page:** Verify the page renders with light/cream backgrounds. Cards should be white on cream. Text should be dark charcoal. Any accent elements should be maroon-toned.
3. **Feed page** (http://localhost:3000/feed): Check that feed cards have white backgrounds with subtle shadows. Text is readable (dark on light). Status/severity chips are readable.
4. **Dashboard** (http://localhost:3000/dashboard): Verify cards and panels render with light backgrounds. No dark glassmorphism remnants.
5. **Map page** (http://localhost:3000/map): Verify map overlays and popups use light styling.
6. **General checks across all pages:**
   - No page has a dark (#121212) background
   - Text is legible (dark text on light backgrounds)
   - Shadows are subtle (not heavy dark-mode shadows)
   - No glass-card elements have dark semi-transparent backgrounds or visible backdrop-blur
7. **Typography:** Headings should appear in Outfit font (rounded, geometric). Body text should be Inter (clean, neutral). Check if there is visual distinction between heading sizes.

Note: Some components will still have inline hardcoded styles (style={{ color: '#...' }}) that are not in scope for Phase 1. Those will be cleaned up in Phases 3-4. The key verification is that the CSS token layer itself is correct.

Resume signal: Type "approved" if the token system looks correct, or describe specific issues (e.g., "feed cards still dark", "text too light on cream").
  </action>
  <verify>User approval of visual output</verify>
  <done>User confirms brand palette renders correctly across all pages with no dark glassmorphism remnants</done>
</task>

</tasks>

<verification>
1. All 13 automated audits pass
2. Build and lint succeed
3. User confirms visual output matches brand palette expectations
</verification>

<success_criteria>
- Automated audits confirm token completeness, legacy alias preservation, glass-card migration, and WCAG-AA status colors
- User visually confirms: cream backgrounds, white cards, dark text, maroon accents, subtle shadows, readable status chips
- No dark glassmorphism remnants on any page
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-token-foundation/01-02-SUMMARY.md`
</output>
